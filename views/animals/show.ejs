<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs') %> 
<body>
    <%- include('../partials/header.ejs') %> 
    <main>
        <div>
            <article>
                <h2><%= animal.species %> - Is Extinct: <%= animal.extinct ? "True" : "False" %></h2>
                <h3>Location: <%=animal.location %></h3>
                <h3>Life Expectancy: <%=animal.lifeExpectancy %></h3>
                <a href="/animals/<%= animal.id %>/edit "><button>Edit</button></a>

                <form action="/animals/<%= animal._id %>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete">
                </form>
                <a href="/animals/"><button>Back to</button></a>
            </article>
        </div>
    </main>
</body>
</html>
